# THE QA PIPELINE — this repository's only per-change pipeline, and it runs for RELEASE REQUESTS
# ALONE. It replaces ci-event-build.yml and ci-event-userflows.yml, both deleted in the commit that
# added this file; ci-event-release.yml and ci-event-upstream-frontend.yml are untouched. The
# reference template with the full argument is qits-ci's docs/ci-event-release-request.yml.
#
# Per-push CI is retired platform-wide: nothing builds a branch, because cd deploys what a RELEASE
# published rather than what a push built. qits-projects keeps, per open release request, a backing
# branch `release/<id>` — an octopus merge of the request's sources, refolded whenever the set
# changes — and publishes `ReleaseRequestChanged` on every successful re-fold. That branch is
# written by qits-githost's merge primitive, which fires no post-receive and therefore publishes no
# `SCMPublishCommit`: without this event the fold exists and nothing builds it.
#
# DECIDE AT MAIN, BUILD AT THE FOLD. The file is discovered, parsed and selected at this
# repository's `main` head; only the run's branch and sha come from the payload. So a release
# request cannot alter the CI that gates it by folding in a commit that edits this file.
#
# ONE FILE, TWO HALVES, and "a red verify must not cost the image" still holds — bought with
# ORDERING plus PER-STEP CLASSIFICATION rather than with two files that cannot share a verdict. The
# gating build is FIRST; the userflow half declares `gating: false`, which still fails the run and
# shows red but does not hold the fold for qits-projects' build gate. Keep it LAST: everything after
# a failing step is SKIPPED.
#
# THE SHA IMAGE IS GONE. The old build step ended in `docker push
# $QITS_REGISTRY/$QITS_IMAGE_REPOSITORY/qits-platform-system:$QITS_CI_SHA`, the per-push coordinate
# cd keyed on; a fold's merged sha is not a coordinate anything could deploy. The image is still
# BUILT here under a local tag — the gate on the fold — and the CalVer coordinate is
# ci-event-release.yml's, untouched.
#
# Loop review: this pipeline PUTs a docs bundle and pushes no commit, no tag and no image — nothing
# the fold is made of changes, so it cannot re-trigger itself. Publish-if-absent: events redeliver
# and docs versions are immutable, so 409 is success.
event: ReleaseRequestChanged

when:
  # repoName, not repoId: since the identity cutover the git host's own coordinate is an opaque
  # UUID, so a name match belongs on the name. The event echoes the public name; a rename updates
  # this line in the same campaign. `suppressCi` went with the push event that carried it — a
  # release request is not a push and `-o qits.no-ci` has nothing to say about one.
  - repoName: { exact: qits-system-platform-service }

checkout:
  branch: backingBranch
  sha: mergedSha

steps:
  # --- the GATING half: build the fold ------------------------------------------------------------
  # ci-event-build.yml's step, minus the push.
  - image: qits/build-images/node-docker-base:latest
    docker: true
    timeout-seconds: 3600
    script: |
      # The daemon runs this with `bash -c` and no -e, so without this a failed docker build is
      # swallowed and the run reports SUCCESS — a green verdict for a fold that does not build is
      # exactly what a release gate must never be told. -u is load-bearing too: an unset registry
      # variable must stop the step rather than quietly composing a tag against nothing.
      set -eu

      # $QITS_CI_BRANCH is release/<id> and $QITS_CI_SHA is the merged sha — the fold is already
      # checked out, so nothing here fetches or switches refs.
      #
      # --- half one: the client -------------------------------------------------------------------
      # The daemon's clone is shallow and does not recurse, so the qits-system-platform-frontend submodule at
      # service/src/main/webui arrives as an empty directory — and the Dockerfile's `test -f` guard
      # stops the image build on exactly that (Quinoa's own `No package.json found in Web UI
      # directory` would stop it a moment later). Initialising it here rather than asking the daemon
      # to clone differently keeps the submodule this repo's business, and the derived URL is what
      # makes the committed relative one resolve against whichever host this run cloned from.
      frontend_url="${QITS_CI_REPOSITORY_URL%/*}/qits-system-platform-frontend"
      git -c submodule.qits-system-platform-frontend.url="$frontend_url" submodule update --init service/src/main/webui

      # THE LOCKFILE is the one file this step edits, and npm leaves no alternative. npm pins a full
      # resolved URL per package, and the SPA's lockfile is generated on the deployment host, so every
      # entry names an edge address that does not exist in this container. `npm ci` fetches tarballs
      # by that URL and never asks the configured registry for them, so the environment below does not
      # reach them: without this swap the install dies on connection refused. The paths are identical
      # on every address, so replacing the ORIGIN and keeping the path is all it takes, and the
      # integrity hashes are what make that safe — bytes from the other address must still hash the
      # same. The checkout is this step's own; nothing is edited that anyone will read again.
      #
      # THE BYTE PLANE IS TWO SERVICES, WHICH IS WHY THE SWAP READS THE PATH. qits-platform-mirror
      # holds the npmjs pull-through cache; qits-artifacts hosts the @qits scope. No single origin
      # serves both, so the path is what tells them apart: an entry under the hosted registry's own
      # pathname — $QITS_NPM_REGISTRY_URL's — is a @qits tarball and gets the hosted origin, and every
      # other entry gets the proxy's.
      #
      # THE ORDER OF THE TWO EXPRESSIONS IS THE WHOLE TRICK. sed applies -e in sequence per line, so
      # the broad proxy swap runs FIRST and the narrow path-anchored one corrects the @qits entries
      # after it. The other order lets the broad expression undo the narrow one.
      #
      # npm's own knob for exactly this, `--replace-registry-host=always`, cannot do it against a
      # registry mounted under a path, and could not have told two registries apart either —
      # qits-workspaces' recipe measures that failure in full.
      cd service/src/main/webui
      npm_hosted_origin=$(node -p 'new URL(process.env.QITS_NPM_REGISTRY_URL).origin')
      npm_proxy_origin=$(node -p 'new URL(process.env.QITS_NPM_PROXY_URL).origin')
      npm_hosted_path=$(node -p 'new URL(process.env.QITS_NPM_REGISTRY_URL).pathname')
      sed -i -E \
        -e "s#(\"resolved\": \")https?://[^/\"]+#\1$npm_proxy_origin#" \
        -e "s#(\"resolved\": \")https?://[^/\"]+($npm_hosted_path)#\1$npm_hosted_origin\2#" \
        package-lock.json

      # ENVIRONMENT, not a file. qits-system-platform-frontend COMMITS an .npmrc (the edge vhosts, right for
      # a developer on the deployment host), and npm ranks a project .npmrc ABOVE ~/.npmrc — so a
      # `cat > ~/.npmrc` preamble would be written and silently ignored. `npm_config_*` environment
      # outranks both files, so it wins without touching the submodule's tree.
      env npm_config_registry="$QITS_NPM_PROXY_URL" \
          "npm_config_@qits:registry=$QITS_NPM_REGISTRY_URL" \
          npm ci --no-audit --no-fund

      # dist/qits-platform-spa-system/browser — the bundle the image build consumes, and the ONE
      # artifact this half exists to produce. Lint and test are qits-system-platform-frontend's own
      # pipeline's job; repeating them here would only make this repo red for someone else's commit.
      npm run build
      cd -

      # --- half two: the image --------------------------------------------------------------------
      # --network host, NOT a custom network: buildkit (the builder format the platform targets — a
      # swarm cannot assume the legacy builder) refuses custom networks on a build. Host networking
      # is buildkit-supported, and the maven registry's host-published address is derived from the
      # same injected env, so no address is stated here. The build needs it because this repo depends
      # on qits-db-core, qits-auth-core and qits-arch-rules.
      #
      # THE CREDENTIAL IS A SECRET MOUNT AND NEVER A BUILD ARG — an arg is recorded in the image
      # history. The launcher injects the run's commissioned idp client as
      # $QITS_COMMISSIONED_CLIENT_ID/$QITS_COMMISSIONED_CLIENT_SECRET; these two flags hand that
      # pair to the mounts on the Dockerfile's mvnw RUN, which is the only place it exists.
      # Unconditional on purpose: a missing env source is NOT an error — measured on buildx v0.36.1,
      # `--secret id=x,env=DOES_NOT_EXIST` builds green and the mounted variable is simply unset —
      # so these lines do nothing at all until a run carries the pair.
      #
      # THE TAG IS LOCAL AND NOTHING IS PUSHED. The registry-qualified `:$QITS_CI_SHA` coordinate was
      # the per-push publication and it is retired with the push path; `docker: true` stays for the
      # daemon socket the build needs and for the pull auth the mirror-fronted base images need.
      ref="qits-platform-system:$QITS_CI_SHA"
      docker build --network host \
        --build-arg QITS_MAVEN_REPOSITORY_URL="http://$QITS_REGISTRY/artifacts/maven/maven" \
        --build-arg QITS_MAVEN_CENTRAL_URL="${QITS_MAVEN_CENTRAL_MIRROR_URL:-}" \
        --secret id=qits-client-id,env=QITS_COMMISSIONED_CLIENT_ID \
        --secret id=qits-client-secret,env=QITS_COMMISSIONED_CLIENT_SECRET \
        -t "$ref" -f docker/Dockerfile .
      # NO `docker rmi "$ref"`: on a CI host it frees nothing — the layers stay in the builder's
      # cache either way — while under the containerd image store it drops the last reference to
      # content a concurrent build of this same Dockerfile may still be resolving.

  # --- the NON-GATING half: verify the stories, and publish what the run observed -----------------
  # ci-event-userflows.yml's step, unchanged but for `gating: false`, which is the whole of what that
  # separate file was.
  #
  # `mvn verify` at the fold — which regenerates service/target/userstories/ through the four story
  # ITs — and the reports published as one docs bundle, site
  # @userflows/qits-system-platform-service, version = the merged sha, branch riding as artifacts
  # metadata for discovery.
  #
  # ONE STEP, BECAUSE STEPS SHARE NOTHING. Every step container clones the repository for itself, so
  # the gating step above cannot hand this one anything: the verify and the publish stay together.
  #
  # THIS STEP IS WHERE THIS REPOSITORY'S SUITE RUNS AT ALL, and that is worth knowing before reading
  # anything below. The image build carries `-DskipTests` in its Dockerfile's mvnw line, so until the
  # userflow file existed every test here had only ever run on a developer's machine. What that means
  # concretely is the PTY suites — system/'s ForeignPtyTest and TerminalCttyTest, service/'s
  # TerminalSocketTest and TerminalLingerTest — which fork real children on real pseudo-terminals
  # through `setsid --ctty`. maven-base was already prepared for exactly that: it installs util-linux
  # precisely because busybox's setsid applet rejects `--ctty`, and its own comment names "the daemon
  # repositories' PTY suites" as the reason. Nothing else in this step is about them; they are simply
  # what `verify` runs on the way to failsafe.
  #
  # `user: build`, and here it is NOT zonky's reason. There is no embedded postgres in this repository
  # and no datasource of any kind — every answer is read from the docker daemon at the moment it is
  # asked — so nothing here refuses to run as uid 0 the way initdb does. It stays `build` because the
  # step forks real children on real terminals and there is no call for those to be root's, and
  # because maven-base carries a passwd-backed uid 1001 (adduser -D) for whichever step wants one.
  # A step container is started --cap-drop=ALL either way; /workspace is 1777 with a system-level
  # safe.directory entry, so a non-root checkout is what the image was built to serve.
  #
  # THERE IS NO DOCKER SOCKET IN THIS STEP AND NONE IS WANTED, in the repository whose whole subject
  # is docker. The suite points `qits.system.docker.binary` at a shell script under
  # src/test/resources/fake-docker — a real ProcessBuilder forks a real child and the argv it gets is
  # the argv the builders produced, so nothing is mocked and no daemon is needed. The story catalogue
  # goes one step further: stories/support/StoryDocker STAGES that same script into
  # target/story-docker/ (a directory only the launched artifact ever writes to, so the recording is
  # not the surefire suite's several hundred calls) and reads back the argv-plus-exit-code log it
  # writes. That is what makes the docker hop an OBSERVED `process` edge rather than a claim, while
  # this step still runs in a container with no daemon and no capability to reach one — the
  # clone-alone rule held in the one place it is hardest to hold. The single edge that stays declared
  # is the unix socket BEHIND the CLI, which no tap can stand in front of. `docker: true` would be a
  # socket grant for nothing.
  #
  # Quinoa is off (`-Dquarkus.quinoa=false` — the extension's root toggle; `.enabled` is silently
  # ignored and npm still runs): the client is the gating step's business, where the
  # qits-system-platform-frontend submodule is initialised and its lockfile origins are swapped against the
  # platform's npm registry, and none of that ritual belongs here — the stories assert nothing about
  # the SPA. It is also what keeps this step from needing the submodule at all: the daemon's clone
  # does not recurse, so service/src/main/webui arrives EMPTY, and an empty directory is the one case
  # Quinoa treats as a misconfiguration rather than as "no client".
  #
  # THE IT FLAGS ARE THE OTHER HALF, and they are why this repository does NOT opt its service
  # module back into ITs the way qits-githost does. `skipITs` is true in the root pom and stays true;
  # this module has five integration tests and exactly one of them does not belong to this run:
  #
  #   TokenValidationBootstrapIT  the catalogue's first class — the packaged boot with the OIDC tenant
  #                               ON, which is also the only place it is ever on
  #   HostOverviewIT              the machine window: the host, its node and the swarm around it
  #   TerminalRefusalIT           who may hold a shell on the platform host, and what a refused one
  #                               costs the daemon (nothing — which is the assertion)
  #   TerminalSessionIT           a real shell and the host monitor, end to end on a real PTY
  #   PackagedSurfaceIT           NOT in the list: half about the CLIENT (the base href at the root,
  #                               the deep links, the fallback that must not swallow /system), which
  #                               the quinoa line below deliberately does not build. A blanket
  #                               `-DskipITs=false` would make this run red on a test that is right.
  #
  # THE LIST IS THE CATALOGUE, BY NAME, AND A NEW STORY CLASS JOINS IT IN THE SAME COMMIT — an
  # unlisted class is silently never run and its diagram silently never published. Its ORDER here is
  # not the run order: failsafe takes the classes as a set, and what orders them is
  # UserflowClassOrderer, registered as junit's secondary orderer in the test application.properties
  # and driven by each story's @UserflowRunsAfter. That ordering is load-bearing rather than tidy —
  # every cumulative source (the idp's request log, StoryDocker's recording) is attributed by a
  # cursor, so the startup JWKS fetch and the boot sweep's two docker calls land in whichever story
  # drains FIRST, which the ordering makes TokenValidationBootstrapIT's first story.
  - image: qits/build-images/maven-base:latest
    user: build
    gating: false
    timeout-seconds: 2400
    script: |
      set -eu
      # The platform registry, exactly as qits-eventstream's step derives it: the pom's default
      # names a host-published address unreachable from a step container, and the exact-id mirror
      # in .qits-maven-settings.xml is what passes Maven's HTTP blocker.
      export QITS_MAVEN_REPOSITORY_URL="$QITS_MAVEN_REGISTRY_URL"
      # Maven Central through the platform mirror's in-network alias — empty (an older qits-ci that
      # injects nothing, or a deployment that turned the mirror plane off) deactivates the settings
      # profile and Central is resolved directly.
      export QITS_MAVEN_CENTRAL_URL="${QITS_MAVEN_PROXY_URL:-}"
      ./mvnw -B -ntp -s .qits-maven-settings.xml verify \
        -Dqits.maven.repository.url="$QITS_MAVEN_REPOSITORY_URL" \
        -Dquarkus.quinoa=false \
        -DskipITs=false \
        "-Dit.test=TokenValidationBootstrapIT,HostOverviewIT,TerminalRefusalIT,TerminalSessionIT"

      # `-C … .` so the story directories are the bundle's roots — nesting one directory deep would
      # put it in every served URL. The path is service/target/userstories because the stories live
      # in the one module that packages an artifact to boot; `system/` is a library jar.
      tar -czf userstories.tgz -C service/target/userstories .

      # Branch and commit ride as metadata (the docs store's exact-match discovery keys); the
      # version is the merged sha itself, so a bundle URL never changes meaning.
      status=$(curl -sS -o /tmp/docs-publish.out -w '%{http_code}' \
        -X PUT --data-binary @userstories.tgz \
        -H "X-Artifacts-Meta-git.branch.name: $QITS_CI_BRANCH" \
        -H "X-Artifacts-Meta-git.commit.hash: $QITS_CI_SHA" \
        -H "X-Artifacts-Meta-git.repository.name: qits-system-platform-service" \
        "$QITS_DOCS_URL/@userflows/qits-system-platform-service/-/$QITS_CI_SHA")
      case "$status" in
        201) echo "published userflows for qits-system-platform-service@$QITS_CI_SHA ($QITS_CI_BRANCH)" ;;
        409) echo "userflows for qits-system-platform-service@$QITS_CI_SHA already published — skipping" ;;
        *)   echo "docs publish failed: HTTP $status"; cat /tmp/docs-publish.out; exit 1 ;;
      esac
